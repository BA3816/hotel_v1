{
  "name": "hotel-booking-system",
  "version": "1.0.0",
  "description": "Hotel booking system with React frontend and Express.js backend",
  "scripts": {
    "dev": "npm-run-all --parallel dev:backend dev:frontend",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "setup": "npm run install:all && cd backend && cp env.example .env && echo 'Please edit backend/.env with your MySQL credentials'"
  },
  "keywords": ["hotel", "booking", "react", "express", "mysql"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}

